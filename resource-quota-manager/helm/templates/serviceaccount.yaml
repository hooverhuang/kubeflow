{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "resource-quota-manager.serviceAccountName" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "resource-quota-manager.labels" . | nindent 4 }}
{{- end }}